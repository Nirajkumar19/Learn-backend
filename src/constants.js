// We want to store the name of database here
// We don't keep in environment variable because we are just gave the name of database. We are not going to use so many times this database name.

export const DB_NAME="PractiseProject1";